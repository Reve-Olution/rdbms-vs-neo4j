--Création des tables    
CREATE TABLE PERSONNES (
    ID INTEGER(10) AUTO_INCREMENT,
    NOM VARCHAR(100),
    PRIMARY KEY(ID)
);

CREATE TABLE AMIS (
    ID INTEGER(10) AUTO_INCREMENT,
    ID_PERSONNES1 INTEGER(10) NOT NULL,
    ID_PERSONNES2 INTEGER(10) NOT NULL,
    PRIMARY KEY(ID)
);